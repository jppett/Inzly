{
  "name": "@bones-report/permits-fetcher",
  "version": "1.0.0",
  "description": "Building permit history via Shovels, used to corroborate what the photo agents see",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "nodemon --exec tsx src/index.ts",
    "start": "node dist/index.js",
    "lookup": "tsx src/cli.ts",
    "clean": "rm -rf dist",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No linting yet\""
  },
  "dependencies": {
    "@bones-report/shared": "workspace:*",
    "kafkajs": "^2.2.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "nodemon": "^3.0.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  }
}
