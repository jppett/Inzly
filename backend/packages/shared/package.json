{
  "name": "@bones-report/shared",
  "version": "1.0.0",
  "description": "Shared types, utilities and event contracts for bones-report",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No linting yet\""
  },
  "dependencies": {
    "zod": "^3.22.0",
    "redis": "^4.6.0",
    "kafkajs": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}