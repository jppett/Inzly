# Permits Fetcher Service Dockerfile
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install pnpm
RUN npm install -g pnpm

# Copy package files for dependency resolution
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml ./
COPY packages/shared/package.json ./packages/shared/
COPY packages/permits-fetcher/package.json ./packages/permits-fetcher/

# Install dependencies
RUN pnpm install --frozen-lockfile

# Copy source code
COPY packages/shared ./packages/shared
COPY packages/permits-fetcher ./packages/permits-fetcher
COPY tsconfig.json ./

# Build the shared package first
RUN pnpm --filter @bones-report/shared run build

# Build the permits-fetcher service
RUN pnpm --filter @bones-report/permits-fetcher run build

# Set user for security
USER node

# Start the permits-fetcher service
CMD ["node", "packages/permits-fetcher/dist/index.js"]